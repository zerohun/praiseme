<% users.each do |user| %>
  <% if is_mine == true %>
    <div class="mine_stamp round shadow">
  <% else %>
    <div class="stamp_people round shadow">
    <% end %>
      <%= link_to user_profile_path(user) do %>
        <div class="image">
          <%= image_tag user.image_url, :class => "img-small img-circle image-shadow" %>
        </div>
        <div class="name">
        <%= user.username %><br />(<%= UserStamp.where(:stamp_id => stamp_id , :user_id => user.id).first.score  %> Points)
        </div>
      <% end %>
    <% if is_mine == false %>
      <div class="button-group pull-right">
        <%= render :partial => '/followings/following_button', :locals => {:user => user} %>
        <%= link_to "Glorify!", new_compliment_path(:compliment => {:receiver_id => user.id}), :class => "btn btn-success btn-bravo" %>
      </div>
    <% end %>
  </div>
<% end %>
