<%= form_for(@user_stamp) do |f| %>
  <% if @user_stamp.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_stamp.errors.count, "error") %> prohibited this user_stamp from being saved:</h2>

      <ul>
      <% @user_stamp.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.object.stamp.title %>
    <%= f.hidden_field :stamp_id %>
  </div>
  <div class="field">
    <%= f.object.user.username %>
    <%= f.hidden_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :score %><br />
    <%= f.number_field :score %>
  </div>
  <div class="field">
    <%= f.label :level %><br />
    <%= f.number_field :level %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
